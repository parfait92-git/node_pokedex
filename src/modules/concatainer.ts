const concatName = (firstName: string, lastName: string): string => {
    return firstName+' '+lastName;
}


module.exports = {
    concatName
}

export const  concatName2  = (firstName: string, lastName: string): string => {
    return firstName+' '+lastName;
}